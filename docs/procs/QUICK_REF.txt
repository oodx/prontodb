====
 ⚡ PRONTODB QUICK REFERENCE - 30 SECOND CONTEXT
====
Updated: 2025-09-21

🎯 **Current Focus**: 🚨 CRITICAL ARCHITECTURAL REBUILD REQUIRED
- DISCOVERY: Shared database architecture is fundamentally broken
- BLOCKER: Each address needs isolated keystore for true multi-agent support
- STATUS: Complete rebuild required - current codebase compromised

📊 **Project Status**:
- ✅ Meta Process v2 implementation complete
- ✅ RSB Framework patterns preserved
- 🚨 CRITICAL: Storage architecture fundamentally flawed
- 🔴 URGENT: Complete rebuild required
- ⬆️ Next: Design per-address keystore architecture

🚨 **Critical Context**:
- **Branch**: admin/meta-process
- **BLOCKER**: Shared SQLite breaks multi-agent isolation
- **DISCOVERY**: Per-address keystores required for correct operation
- **IMPACT**: Current multi-agent claims are false - architecture broken

📋 **Current Tasks** (see docs/procs/SPRINT.txt):
1. Complete Meta Process v2 documentation structure
2. Migrate existing docs to new locations
3. Create validation automation
4. Test and refine workflow

🏗️ **Architecture**:
- **Language**: Rust with RSB framework
- **Backend**: SQLite with WAL mode
- **Pattern**: String-biased APIs throughout
- **Storage**: XDG-compliant paths (~/.local/share/prontodb/)

🔧 **Key Commands**:
```bash
cargo build --release       # Build optimized binary
cargo test                  # Run unit tests
./bin/test.sh              # Full test suite
./bin/deploy.sh            # Production deployment
./bin/validate-docs.sh     # Check doc health
```

⚡ **Immediate Next Steps**:
1. Build core storage modules (keystore.rs, paths.rs, address.rs)
2. Implement filesystem-first addressing architecture
3. Create per-address keystore system

💡 **FUTURE DEPENDENCY NOTE**: RSB will eventually forward hub itself, eliminating dual deps

🚨 **Critical Rules**:
- Always use RSB patterns (bootstrap! → options! → dispatch!)
- Update CONTINUE.md before closing session
- Run tests before significant changes
- Follow string-biased API patterns
- Document architectural decisions

====